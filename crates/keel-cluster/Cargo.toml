[package]
name = "keel-cluster"
version = "0.1.0"
edition = "2021"

[dependencies]
keel-proto = { path = "../keel-proto" }
tonic = "0.11"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
